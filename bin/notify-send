#!/usr/bin/env bash

ICON=~/.local/pixmaps/alert.xpm
FONT="-*-dina-medium-r-*-*-13-*-*-*-*-*-*-*"

PIPE="/tmp/dnotipipe"

if [ ! -e "$PIPE" ]; then
  mkfifo "$PIPE"
  (dzen2 -p 5 < "$PIPE"
   rm -f "$PIPE") &
fi

#echo "$*" | dzen2 -p 5

(echo "^i(${ICON}) $*"; sleep 1) > "$PIPE"
